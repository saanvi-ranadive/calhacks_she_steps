"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_plugins_1 = require("@expo/config-plugins");
const withLiveKit = (config, options) => {
    if (options) {
        let androidOptions = options.android;
        if (androidOptions) {
            let audioType = androidOptions.audioType;
            if (audioType) {
                config = (0, config_plugins_1.withAndroidManifest)(config, config => {
                    const mainApplication = config_plugins_1.AndroidConfig.Manifest.getMainApplicationOrThrow(config.modResults);
                    config_plugins_1.AndroidConfig.Manifest.addMetaDataItemToMainApplication(mainApplication, 'io.livekit.reactnative.expo.ANDROID_AUDIO_TYPE', audioType);
                    return config;
                });
            }
        }
    }
    return config;
};
exports.default = withLiveKit;
